# Please insure that your custom_dataset are put in same parent dir with YOLOv6_DIR
train: ../e/images/train # train images
val: ../e/images/val # val images
test: ../e/images/test # test images (optional)

# whether it is coco dataset, only coco dataset should be set to True.
is_coco: False
# Classes
nc: 7  # number of classes
names: ['Feeding', 'Drinking', 'Perching', 'Pecking', 'Dust Bathing', 'Preening', 'Foraging']  # class names
